# Default: run the graph with deterministic mock LLM responses (no API key needed).
ISO_PULSE_USE_MOCK_LLM=true

# Used only when ISO_PULSE_USE_MOCK_LLM=false
OPENAI_API_KEY=
ISO_PULSE_MODEL=gpt-4o-mini
ISO_PULSE_TEMPERATURE=0
